# Better Auth Configuration
# SITE_URL for local development
SITE_URL=http://localhost:3000

# Convex Backend
# Get this from https://dashboard.convex.dev
NEXT_PUBLIC_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Site URL (same as NEXT_PUBLIC_CONVEX_URL but ends in .site)
NEXT_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# Better Auth Secret (set in Convex environment)
# Generate with: openssl rand -base64 32
# Store in Convex: npx convex env set BETTER_AUTH_SECRET your_generated_secret
# BETTER_AUTH_SECRET is stored in Convex, not here

# Site URL for Better Auth (set in Convex environment)
# Store in Convex: npx convex env set SITE_URL http://localhost:3000
# SITE_URL is also stored in Convex, not here

# X/Twitter OAuth 2.0 Credentials
# Get these from https://developer.twitter.com/en/portal/dashboard
# Note: Store these in Convex Environment Variables, NOT in this .env file
# TWITTER_CLIENT_ID=your_client_id
# TWITTER_CLIENT_SECRET=your_client_secret
# TWITTER_REDIRECT_URI=http://localhost:3000/api/auth/twitter/callback

# Encryption Key for OAuth Token Storage
# Generate with: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
# IMPORTANT: Store this in Convex Environment Variables, NOT in this .env file
# ENCRYPTION_KEY=your_base64_encoded_256_bit_key
